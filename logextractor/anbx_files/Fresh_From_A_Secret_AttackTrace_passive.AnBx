Protocol: Fresh_From_A_Secret_AttackTrace_passive AnB

Types:
	Agent I,A,B;
	Number Msg,Nonce;
	Function [Agent -> PublicKey] pk,sk;

Knowledge:
	I: A,B,I,pk,sk,inv(pk(I)),inv(sk(I));
	A: A,I,pk,sk,inv(pk(A)),inv(sk(A)),B;
	B: I,B,pk,sk,inv(pk(B)),inv(sk(B)),A

Actions:
	A -> I: A
	I -> B: A

	B -> I: {Nonce,B}pk(A)
	I -> A: {Nonce,B}pk(A)

	A -> I: {{Nonce,B,Msg}inv(sk(A))}pk(B)
	I -> B: {{Nonce,B,Msg}inv(sk(A))}pk(B)

Goals:

	Msg secret between A,B
