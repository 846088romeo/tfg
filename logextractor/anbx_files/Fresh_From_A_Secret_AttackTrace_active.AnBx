Protocol: Fresh_From_A_Secret_AttackTrace_active AnB

Types:
	Agent I,A,B;
	Number Msg,Nonce;
	Function [Agent -> PublicKey] pk,sk;

Knowledge:
	I: A,B,I,pk,sk,inv(pk(I)),inv(sk(I));
	A: A,I,pk,sk,inv(pk(A)),inv(sk(A)),B;
	B: I,B,pk,sk,inv(pk(B)),inv(sk(B)),A

Actions:

	A -> I: A
	I -> A: {Nonce,I}pk(A)
	A -> I: {{Nonce,I,Msg}inv(sk(A))}pk(I)

Goals:
	Msg secret between A
