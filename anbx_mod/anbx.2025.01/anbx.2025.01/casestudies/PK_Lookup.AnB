Protocol: PK_Lookup

Types:
	Agent A,B;
	Number Msg;
	Function pk;		# Agent -> PublicKey

Knowledge:
	A: A,B,inv(pk(A));
	B: B,pk

Actions:
	A -> B: A
	B -> A: {Msg}pk(A)

Goals:
	B ->* A: Msg
	inv(pk(A)) secret between A
