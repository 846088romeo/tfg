Protocol: Secret_for_B

Types:
	Agent A,B;
	Number Msg;
	Function pk;		# Agent -> PublicKey

Knowledge:
	A: A,B,pk;
	B: inv(pk(B))

Actions:
	A -> B: {Msg}pk(B)

Goals:
	A ->* B: Msg
	Msg secret between A,B
	inv(pk(B)) secret between B
